---
phase: 10-staff-scheduling-clock-in-out
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
files_modified:
  - app/src/pages/StaffDashboard.tsx
autonomous: true
requirements:
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-06

must_haves:
  truths:
    - "MyScheduleTab shows upcoming shifts for the logged-in staff member in a clear list"
    - "Staff can set their availability preferences (available Y/N per day, preferred times) from MyScheduleTab"
    - "Staff can submit a shift swap request naming a target staff member from their upcoming shifts"
    - "Staff can submit a time-off request with date range, leave type, and reason from MyScheduleTab"
    - "Submitted requests show their current status (pending/approved/denied) in the same tab"
  artifacts:
    - path: "app/src/pages/StaffDashboard.tsx"
      provides: "Fully implemented MyScheduleTab with shifts, availability, swap requests, time-off requests"
  key_links:
    - from: "MyScheduleTab shifts list"
      to: "trpc.scheduling.getMyShifts"
      via: "staff token from useStaffAuth"
    - from: "MyScheduleTab availability"
      to: "trpc.shiftManagement.setAvailability / getMyAvailability"
      via: "staff token"
    - from: "MyScheduleTab swap request form"
      to: "trpc.shiftManagement.requestShiftSwap"
      via: "fromShiftId selected from shifts list"
    - from: "MyScheduleTab time-off form"
      to: "trpc.shiftManagement.requestTimeOff / getMyTimeOffRequests"
      via: "staff token"
---

<objective>
Fully implement the MyScheduleTab component in StaffDashboard.tsx — it currently exists but needs availability preferences, swap requests, and time-off request functionality completed.

Purpose: Staff need to view their roster, declare when they can work, and submit leave/swap requests — all from the existing MyScheduleTab location in StaffDashboard.

Output: Expanded MyScheduleTab function in app/src/pages/StaffDashboard.tsx.
</objective>

<execution_context>
@C:/Users/Main PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Main PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/src/pages/StaffDashboard.tsx
@app/api/scheduling-router.ts
@app/api/shift-management-router.ts

<interfaces>
<!-- Existing MyScheduleTab function — read from StaffDashboard.tsx lines 5004–5410 -->
The function already exists and has some structure. Before implementing, read the current body
to understand what is already rendered and what is stub/incomplete. Key findings to expect:
- It probably renders upcoming shifts list
- Time-off submission form may be partially present
- Availability grid likely not present
- Swap request form likely not present

<!-- tRPC procedures for MyScheduleTab -->
trpc.scheduling.getMyShifts.useQuery({ token, weeksAhead: 2 })
  → [{ id, venueId, staffId, shiftDate, startTime, endTime, role, notes, createdAt }]

trpc.shiftManagement.getMyAvailability.useQuery({ token })
  → Array of 7 items for Sun–Sat, each: { dayOfWeek, available, preferredStartTime, preferredEndTime, note }

trpc.shiftManagement.setAvailability.useMutation({ token, dayOfWeek, available, preferredStartTime?, preferredEndTime?, note? })
  → { ok }

trpc.shiftManagement.requestTimeOff.useMutation({ token, startDate, endDate, leaveType, reason? })
  → { id }

trpc.shiftManagement.getMyTimeOffRequests.useQuery({ token })
  → [{ id, startDate, endDate, leaveType, reason, status, reviewedAt, createdAt }]

trpc.shiftManagement.requestShiftSwap.useMutation({ token, fromShiftId, targetStaffId?, targetShiftId?, reason? })
  → { id }

trpc.scheduling.listStaff.useQuery({ token })
  → [{ id, name, username, role, isActive }]
  (used to populate "Target Staff" select in swap form)

<!-- StaffDashboard shared styles -->
The tab components in StaffDashboard use inline styles with these common patterns:
  Card wrapper: { background: '#fff', borderRadius: '12px', border: '1px solid #e7e5e4', padding: '24px', marginBottom: '16px' }
  Section title: { fontSize: '16px', fontWeight: 700, color: '#1c1917', marginBottom: '16px' }
  Label: { fontSize: '11px', color: '#78716c', display: 'block', marginBottom: '4px', fontWeight: 600 }
  Input: { width: '100%', padding: '10px 12px', borderRadius: '8px', border: '1px solid #e7e5e4', fontSize: '13px', boxSizing: 'border-box' as const }
  Primary button: { padding: '9px 20px', background: '#1c1917', color: '#fafaf9', border: 'none', borderRadius: '8px', fontSize: '13px', fontWeight: 600, cursor: 'pointer' }
  Secondary button: { padding: '8px 16px', background: 'transparent', color: '#78716c', border: '1px solid #e7e5e4', borderRadius: '8px', fontSize: '13px', cursor: 'pointer' }

Status badge colors:
  pending: background '#fef3c7', color '#92400e'
  approved: background '#dcfce7', color '#166534'
  denied: background '#fee2e2', color '#991b1b'

Days of week labels: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing MyScheduleTab to understand current state</name>
  <files>app/src/pages/StaffDashboard.tsx</files>
  <action>
Read the MyScheduleTab function body (approximately lines 5004–5410) before making any edits.
Identify:
1. What data queries already exist in the function
2. What UI sections are already rendered (shifts list, time-off form, etc.)
3. What state variables already exist
4. Which tRPC mutations are already wired

Then make ONLY the additive/replacement edits needed — do not remove working code.
This is an analysis + targeted-edit task.
  </action>
  <verify>
    <automated>cd "C:/Users/Main PC/Documents/Cafe App" && grep -n "function MyScheduleTab" app/src/pages/StaffDashboard.tsx</automated>
  </verify>
  <done>Executor has read the existing function and understands the current implementation scope.</done>
</task>

<task type="auto">
  <name>Task 2: Complete MyScheduleTab with availability, swap, and time-off sections</name>
  <files>app/src/pages/StaffDashboard.tsx</files>
  <action>
Replace or expand the MyScheduleTab function in StaffDashboard.tsx to include all four sections described below. Keep any existing working code; only add missing sections.

SECTION 1 — Upcoming Shifts (ensure exists and is correct):
Query: trpc.scheduling.getMyShifts.useQuery({ token, weeksAhead: 2 })
Display: card with list of upcoming shifts sorted by shiftDate ascending.
Each shift row: "[shiftDate] — [startTime]–[endTime] [role?]"
  Format shiftDate as: new Date(shift.shiftDate + 'T00:00:00').toLocaleDateString('en-AU', { weekday: 'short', day: 'numeric', month: 'short' })
Each shift row should have a "Request Swap" button that opens the swap form (see Section 3) pre-filled with fromShiftId.
Empty state: "No upcoming shifts scheduled."

SECTION 2 — My Availability:
Query: trpc.shiftManagement.getMyAvailability.useQuery({ token })
Mutation: trpc.shiftManagement.setAvailability.useMutation

UI: 7 rows (one per day of week, Sun–Sat).
Each row:
  [Day label "Mon"] [Toggle: Available/Unavailable checkbox or toggle switch]
  When available: [Preferred from: time input] [Preferred to: time input] [Note: short text]
  "Save" button per row (calls setAvailability.mutate for that dayOfWeek)

State: local copy of availability rows, initialized from query data.
Use state array: availPrefs: Array<{ dayOfWeek, available, preferredStartTime, preferredEndTime, note }>
One setAvailability mutation instance, called with the specific day's data.
Show a per-row success indicator ("Saved" in green for 2s) after successful mutation.

SECTION 3 — Shift Swap Requests:
Substate: swapForm: { fromShiftId: number | null, targetStaffId: number | '', reason: string }
When a shift's "Request Swap" button is clicked, set swapForm.fromShiftId.

Show form as a modal card or inline expansion when swapForm.fromShiftId is set:
  - "Swap shift on [date]" heading
  - "Target staff member" select (from trpc.scheduling.listStaff.useQuery({ token })) — optional
  - "Reason" textarea
  - "Submit" button → trpc.shiftManagement.requestShiftSwap.useMutation({ token, fromShiftId, targetStaffId, reason })
  - "Cancel" button

Below, list all submitted swap requests from trpc.shiftManagement.requestShiftSwap — but this procedure does not exist as a query on the staff side. Use trpc.shiftManagement.getMyTimeOffRequests pattern. NOTE: there is no getMySwapRequests procedure. Add a display note: "Your swap requests are shown to your manager."
Just show a success toast "Swap request submitted!" after mutation succeeds.

SECTION 4 — Time-Off Requests:
This section may already exist. Ensure:
- Form fields: startDate (date input), endDate (date input), leaveType (select: annual/sick/unpaid/other), reason (textarea)
- Submit: trpc.shiftManagement.requestTimeOff.useMutation
- History list: trpc.shiftManagement.getMyTimeOffRequests.useQuery({ token })
  Show each request with: period, leave type, status badge (pending/approved/denied), and denied reason if notes present.

Add a section header "Time-Off Requests" with CalendarOff icon (already imported).
  </action>
  <verify>
    <automated>cd "C:/Users/Main PC/Documents/Cafe App" && npx tsc --noEmit -p app/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>tsc passes; MyScheduleTab renders all four sections: upcoming shifts, availability preferences, swap request form, and time-off request form + history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staff JWT → requestShiftSwap | staffId in JWT — staff can only request swap for their own shifts (backend verifies) |
| Staff JWT → requestTimeOff | staffId in JWT — staff create their own requests only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | requestShiftSwap fromShiftId | mitigate | Backend verifies fromShiftId.staffId matches JWT staffId before inserting |
| T-10-12 | Spoofing | setAvailability | mitigate | staffId from JWT, not from input — staff cannot set another person's availability |
| T-10-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
Run after all tasks:
- npx tsc --noEmit -p app/tsconfig.json exits 0
- grep "getMyAvailability" app/src/pages/StaffDashboard.tsx returns a match
- grep "setAvailability" app/src/pages/StaffDashboard.tsx returns a match
- grep "requestShiftSwap" app/src/pages/StaffDashboard.tsx returns a match
- grep "requestTimeOff" app/src/pages/StaffDashboard.tsx returns a match
- grep "getMyTimeOffRequests" app/src/pages/StaffDashboard.tsx returns a match
</verification>

<success_criteria>
- TypeScript compiles without errors
- Staff can see their next 2 weeks of shifts in MyScheduleTab
- Staff can toggle availability per day and save preferred hours
- Staff can submit a swap request from a shift row — confirmation shown on success
- Staff can submit a time-off request with date range and type
- Submitted time-off requests appear in history with status badges
</success_criteria>

<output>
Create `.planning/phases/10-staff-scheduling-clock-in-out/10-04-SUMMARY.md` when done.
</output>
