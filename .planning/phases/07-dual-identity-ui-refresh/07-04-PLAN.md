---
phase: 07-dual-identity-ui-refresh
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - app/src/pages/Landing.tsx
  - app/src/pages/Login.tsx
  - app/src/pages/Onboarding.tsx
  - app/src/pages/StaffLogin.tsx
  - app/src/pages/SuperAdmin.tsx
autonomous: false
must_haves:
  truths:
    - "Landing page reads as a SaaS product page with a hero (headline + subheadline + CTA buttons), a feature grid of 6 cards, and a social-proof section"
    - "Login is a clean centered card with a visible link to onboarding"
    - "Onboarding is a multi-step form with a visible step progress indicator"
    - "StaffLogin and SuperAdmin have clean, consistent operator-style layouts"
  artifacts:
    - path: "app/src/pages/Landing.tsx"
      provides: "SaaS landing: hero + 6-card feature grid + social proof"
      min_lines: 120
    - path: "app/src/pages/Onboarding.tsx"
      provides: "Multi-step onboarding with progress indicator"
      contains: "step"
  key_links:
    - from: "app/src/pages/Login.tsx"
      to: "/onboarding"
      via: "react-router Link"
      pattern: "/onboarding"
    - from: "app/src/pages/Landing.tsx"
      to: "/login or /onboarding"
      via: "CTA buttons / Links"
      pattern: "/(login|onboarding)"
---

<objective>
Rebuild the marketing and auth surface in a modern SaaS style: a real Landing page (hero + 6-card feature grid + social proof), clean centered Login and StaffLogin forms, a stepped Onboarding flow with a progress indicator, and a tidy SuperAdmin table layout using operator design values.

Purpose: Deliver Success Criterion 6 (Landing reads as a SaaS product page) and bring auth/admin surfaces up to the Dual Identity standard. UI only — no auth logic or tRPC changes.
Output: Updated Landing, Login, Onboarding, StaffLogin, SuperAdmin pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dual-identity-ui-refresh/CONTEXT.md

<interfaces>
<!-- UI only. Preserve every existing auth handler, form submit, and tRPC/mutation call. -->

Routing uses react-router v7. Use `import { Link } from 'react-router'` for internal links
(per prior project decision: Login uses react-router Link to /onboarding, not <a>).

Landing.tsx is currently only ~137 lines — it is the one page that genuinely needs a full rebuild.
Login.tsx (~252 lines), Onboarding.tsx (~1047 lines, already multi-section), StaffLogin.tsx (~621 lines),
SuperAdmin.tsx (~189 lines) need polish, NOT full rewrites — keep their existing handlers/mutations.

Design language:
- Marketing/auth: modern SaaS, clean. Hero with headline + subheadline + two CTA buttons.
- Feature grid 6 cards: Online Ordering, Kitchen Display, Staff Management, Analytics, Gift Cards, Loyalty (use lucide-react icons already in the project).
- Operator values for SuperAdmin/StaffLogin: bg #FAFAFA, card #FFFFFF, border #E4E4E7, accent #5E8B8B,
  text #09090B, secondary #71717A, radius 12 cards / 8 inputs. Use literal values (this plan does not
  depend on 07-01 tokens.css).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Landing as a SaaS product page</name>
  <files>app/src/pages/Landing.tsx</files>
  <read_first>
    - app/src/pages/Landing.tsx (current content — preserve any existing routing/CTA targets and brand text)
    - app/src/pages/Login.tsx (to match the CTA target routes /login and /onboarding)
  </read_first>
  <action>
    Rebuild Landing.tsx as a modern SaaS marketing page with three required sections:

    1) Hero: a centered or left-aligned headline (large, ~clamp 32-56px), a subheadline paragraph, and two CTA buttons — a primary "Start free" linking to /onboarding and a secondary "Sign in" linking to /login (react-router Link). Include a product-mockup placeholder block (a styled box or image-frame div) to the side/below.

    2) Feature grid: exactly 6 feature cards in a responsive grid (3 columns desktop, 1-2 mobile): Online Ordering, Kitchen Display, Staff Management, Analytics, Gift Cards, Loyalty. Each card has a lucide icon, a title, and a one-line description.

    3) Social proof: a section with either 3 testimonial cards (quote + name + role) or a "trusted by" logo row — pick testimonial cards (3 cards).

    Add a simple top nav bar with the B1 brand and a "Sign in" link. Keep it responsive at 375px. No tRPC/auth changes.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/src/pages/Landing.tsx','utf8'); const feats=['Online Ordering','Kitchen Display','Staff Management','Analytics','Gift Cards','Loyalty']; const missing=feats.filter(f=>!c.includes(f)); const cta=/\/onboarding/.test(c)&&/\/login/.test(c); if(missing.length){console.error('missing features',missing);process.exit(1)} if(!cta){console.error('CTA links missing');process.exit(1)} if(c.length<3000){console.error('file too small for a real rebuild');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Landing.tsx contains all six feature labels: Online Ordering, Kitchen Display, Staff Management, Analytics, Gift Cards, Loyalty
    - Landing.tsx contains CTA Links to both `/onboarding` and `/login`
    - Landing.tsx has a hero section, a 6-card feature grid, and a 3-card social-proof section
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Landing reads as a SaaS product page: hero + CTAs, 6-feature grid, and social proof.</done>
</task>

<task type="auto">
  <name>Task 2: Clean Login + Onboarding step progress</name>
  <files>app/src/pages/Login.tsx, app/src/pages/Onboarding.tsx</files>
  <read_first>
    - app/src/pages/Login.tsx (existing login mutation/handler and the /onboarding Link — preserve them)
    - app/src/pages/Onboarding.tsx (existing form sections and submit logic — find the current step/section structure to attach a progress indicator without breaking submission)
  </read_first>
  <action>
    Login.tsx: present as a centered card (maxWidth ~400, margin auto, card bg #FFFFFF, radius 12, subtle shadow, page bg #FAFAFA). Clean email + password inputs (8px radius, labeled), a primary submit button, and a visible "Don't have an account? Start free →" link to /onboarding (react-router Link). Keep the existing login mutation/handler and redirect behavior unchanged.

    Onboarding.tsx: ensure the flow is presented as discrete steps with a visible progress indicator at the top (e.g. "Step 1 of 3" plus a segmented progress bar or numbered dots). If the page already has step state, attach the indicator to it; if it is a single long form, group the fields into 3 logical steps (1: cafe name + slug, 2: contact + password, 3: confirmation) with Next/Back navigation, preserving the existing final submit/mutation exactly. Do not change validation rules or the submit payload.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const l=fs.readFileSync('app/src/pages/Login.tsx','utf8'); const o=fs.readFileSync('app/src/pages/Onboarding.tsx','utf8'); const loginok=/\/onboarding/.test(l); const stepok=/[Ss]tep/.test(o); if(!loginok){console.error('Login missing /onboarding link');process.exit(1)} if(!stepok){console.error('Onboarding missing step indicator');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Login.tsx is a centered card and retains a react-router Link to `/onboarding` and its existing login mutation/handler
    - Onboarding.tsx shows a step/progress indicator (the word "Step" or numbered step dots appear) and preserves its existing final submit/mutation and validation
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Login is a clean centered card linking to onboarding; Onboarding shows a multi-step progress indicator with unchanged submit logic.</done>
</task>

<task type="auto">
  <name>Task 3: Polish StaffLogin + SuperAdmin layouts</name>
  <files>app/src/pages/StaffLogin.tsx, app/src/pages/SuperAdmin.tsx</files>
  <read_first>
    - app/src/pages/StaffLogin.tsx (existing credential/PIN form + auth handler — preserve)
    - app/src/pages/SuperAdmin.tsx (existing table + data queries — preserve)
  </read_first>
  <action>
    StaffLogin.tsx: present a clean centered credential form (operator values: bg #FAFAFA, card #FFFFFF, border #E4E4E7, radius 12, inputs radius 8, accent #5E8B8B). Keep whatever credential/PIN mechanism exists and its existing auth handler unchanged — visual polish only.

    SuperAdmin.tsx: apply a clean operator table layout — header row with secondary text color #71717A, row borders #E4E4E7, comfortable cell padding, card-wrapped table with radius 12 and subtle shadow, page bg #FAFAFA. Preserve every existing data query and action handler. Do not change the columns' data, only their presentation.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const f of ['app/src/pages/StaffLogin.tsx','app/src/pages/SuperAdmin.tsx']){ if(!fs.existsSync(f)){console.error('missing',f);process.exit(1)} } console.log('files present — build is the real gate')"</automated>
  </verify>
  <acceptance_criteria>
    - StaffLogin.tsx retains its existing auth handler/credential mechanism (git diff shows only UI/style edits)
    - SuperAdmin.tsx retains all existing data queries and action handlers; only presentation changed
    - Both files use the operator palette (#FAFAFA / #FFFFFF / #E4E4E7 / #5E8B8B appear)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>StaffLogin and SuperAdmin use clean operator-style layouts with no behavioral changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Marketing + auth refresh: SaaS Landing (hero + 6-feature grid + social proof), centered Login with onboarding link, stepped Onboarding with progress indicator, polished StaffLogin and SuperAdmin.</what-built>
  <how-to-verify>
    1. Run `npm run dev:all`. Visit `/` — confirm a hero with two CTAs, a 6-card feature grid, and a social-proof section; check responsiveness at 375px.
    2. Click "Start free" → lands on /onboarding; click "Sign in" → lands on /login.
    3. On /login confirm the centered card and the onboarding link; attempt a login to confirm the handler still works.
    4. On /onboarding confirm a visible step progress indicator and that completing the flow still creates an account (submit unchanged).
    5. Visit the staff login and super admin pages — confirm clean layouts and that login/admin actions still function.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- Landing has hero + 6 named feature cards + 3 social-proof cards + CTAs to /onboarding and /login
- Login centered card retains /onboarding link and login handler
- Onboarding shows step progress; submit unchanged
- StaffLogin + SuperAdmin polished with operator palette, handlers preserved
- `npm run build` succeeds
</verification>

<success_criteria>
- Success Criterion 6: Landing reads as a SaaS product page (hero, feature grid, social proof) — MET
- Auth + admin surfaces brought to Dual Identity standard with no behavioral changes
</success_criteria>

<output>
Create `.planning/phases/07-dual-identity-ui-refresh/07-04-SUMMARY.md` when done.
</output>
