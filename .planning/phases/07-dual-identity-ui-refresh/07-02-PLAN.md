---
phase: 07-dual-identity-ui-refresh
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/api/venue-router.ts
  - app/src/pages/OwnerDashboard.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "OwnerDashboard renders inside AppShell with a collapsible sidebar (240px ↔ 56px icon-only)"
    - "A dark mode toggle in the top bar persists to localStorage and recolors the operator UI"
    - "The Analytics tab shows a revenue line chart with a 7d/30d range toggle and a top-5 items table"
    - "An activity feed shows the last 5 orders and an unread review badge count"
  artifacts:
    - path: "app/api/venue-router.ts"
      provides: "getActivityFeed procedure returning recent orders + unread review count"
      contains: "getActivityFeed"
    - path: "app/src/pages/OwnerDashboard.tsx"
      provides: "AppShell-wrapped dashboard with collapsible sidebar, dark mode, activity feed, 7d/30d analytics"
      contains: "AppShell"
  key_links:
    - from: "app/src/pages/OwnerDashboard.tsx"
      to: "trpc.analytics.getDailyRevenue"
      via: "useQuery with days range state"
      pattern: "getDailyRevenue"
    - from: "app/src/pages/OwnerDashboard.tsx"
      to: "trpc.venue.getActivityFeed"
      via: "useQuery"
      pattern: "getActivityFeed"
    - from: "app/src/pages/OwnerDashboard.tsx"
      to: "app/src/components/layout/AppShell.tsx"
      via: "import + wrap"
      pattern: "AppShell"
---

<objective>
Adopt the 07-01 operator shell in OwnerDashboard: replace the inline top bar + sidebar with `<AppShell>` + `<SidebarNav>` (collapsible), wrap the app in ThemeProvider for a working dark mode toggle, add a 7d/30d range toggle to the existing Analytics tab, and add an activity feed (last 5 orders + unread review badge) backed by a new `getActivityFeed` tRPC procedure.

Purpose: Deliver Success Criteria 1, 2, and 7 (collapsible sidebar, analytics revenue chart + top-5 table, persisted dark mode) plus the activity-feed feature, while PRESERVING all existing tab logic and tRPC calls in this 5697-line file.
Output: New `getActivityFeed` procedure in venue-router; OwnerDashboard re-shelled with collapsible sidebar, dark mode, range-toggled analytics, and an activity feed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dual-identity-ui-refresh/CONTEXT.md
@.planning/phases/07-dual-identity-ui-refresh/07-01-SUMMARY.md

<interfaces>
<!-- CRITICAL: This is a 5697-line file. Do NOT rewrite it. Make surgical, targeted edits only. -->

OwnerDashboard.tsx current structure (verified):
- Default export `OwnerDashboard()`; token = localStorage.getItem('b1-owner-token') || ''
- activeTab useState union (~30 tab ids incl. 'overview','analytics','pl','menu','settings',...)
- Inline top header bar at lines ~52-117 (56px, brand + venue switcher + view-site links + avatar + logout)
- Inline dark <nav> sidebar at lines ~123-186 (240px, grouped icon+label buttons; active = borderLeft 3px #5E8B8B + bg rgba(255,255,255,0.1))
- <main> content at lines ~189-215 renders each tab component conditionally on activeTab
- AnalyticsTab component (~line 376) ALREADY queries trpc.analytics.getOverview / getDailyRevenue / getTopItems / getHourlyDistribution and renders recharts AreaChart/BarChart. These analytics PROCEDURES ALREADY EXIST in app/api/analytics-router.ts — do NOT create getAnalytics.

Existing analytics procedure signatures (app/api/analytics-router.ts, mounted as trpc.analytics.*):
  getDailyRevenue({ token, days: 7..90 }) -> { date, revenue, orders }[]
  getTopItems({ token, days, limit }) -> { name, quantity, revenue }[]
  getOverview({ token, days }) -> { totalRevenue, orderCount, avgOrder, loyaltyMembers }

From 07-01 (see 07-01-SUMMARY.md for exact props):
  AppShell({ groups, activeId, onSelect, topBarLeft?, topBarRight?, children })
  SidebarNav (composed inside AppShell)
  ThemeProvider / useTheme  (wrap OwnerDashboard's returned tree in ThemeProvider)
  StatCard({ label, value, delta?, icon? })

venue-router pattern (app/api/venue-router.ts): every procedure uses publicQuery, takes
token: z.string(), runs `const payload = await jwtVerify(input.token, JWT_SECRET, { clockTolerance: 60 })`,
reads `payload.payload.venueId`. JWT_SECRET = new TextEncoder().encode(env.jwtSecret).
Reviews table has an unread/read concept — confirm the exact column name when reading the schema
(do NOT assume; grep the reviews table definition for a read/seen/viewed boolean; if none exists,
treat "unread" as reviews created in the last 7 days).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getActivityFeed procedure to venueRouter</name>
  <files>app/api/venue-router.ts</files>
  <read_first>
    - app/api/venue-router.ts (read the existing listOrders / listReviews procedures to copy the exact db query style, table imports, JWT verify block, and return shape)
    - app/api/analytics-router.ts lines 1-50 (for the canonical jwtVerify + venueId + getDb pattern)
    - app/db/schema.ts (find the orders table and the reviews table; confirm whether reviews has a read/seen boolean column — this determines the unread definition)
  </read_first>
  <action>
    Add a new `getActivityFeed` procedure to venueRouter using publicQuery. Input `z.object({ token: z.string() })`. Verify JWT and derive venueId exactly as other procedures do (jwtVerify with clockTolerance 60, payload.payload.venueId).

    Return `{ recentOrders, unreadReviews }`:
    - recentOrders: the 5 most recent orders for venueId ordered by createdAt desc, each `{ id, orderNumber, status, totalAmount, createdAt }` (totalAmount as string, matching existing order shapes).
    - unreadReviews: an integer count. If the reviews table has a boolean read/seen/viewed column, count rows where it is false for this venueId; otherwise count reviews created in the last 7 days for this venueId. Implement whichever the schema supports based on the read_first inspection, and add a one-line code comment stating which definition was used.

    Do NOT modify any existing procedure. Append the new procedure into the existing venueRouter object.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/api/venue-router.ts','utf8'); if(!c.includes('getActivityFeed')){console.error('getActivityFeed missing');process.exit(1)} if(!c.includes('recentOrders')||!c.includes('unreadReviews')){console.error('return shape missing');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - venue-router.ts contains a `getActivityFeed` procedure using publicQuery and taking `token: z.string()`
    - The procedure returns an object containing `recentOrders` (limited to 5, ordered by createdAt desc) and `unreadReviews` (a numeric count)
    - The procedure verifies the JWT and scopes all queries to the token's venueId
    - No existing procedure in venue-router.ts was removed or altered (git diff shows only additions)
    - `npx tsc --noEmit -p app/tsconfig.server.json` (or the server tsconfig) reports no new errors
  </acceptance_criteria>
  <done>trpc.venue.getActivityFeed returns the last 5 orders + an unread review count, scoped to the owner's venue.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-shell OwnerDashboard with AppShell + dark mode + activity feed + 7d/30d analytics</name>
  <files>app/src/pages/OwnerDashboard.tsx</files>
  <read_first>
    - app/src/pages/OwnerDashboard.tsx lines 1-220 (default export, top bar, inline sidebar, main content — the regions being replaced)
    - app/src/pages/OwnerDashboard.tsx around AnalyticsTab (~line 376) (existing analytics queries to add the days-range toggle to)
    - app/src/components/layout/AppShell.tsx and SidebarNav.tsx (07-01 — exact props)
    - app/src/components/layout/ThemeContext.tsx (07-01 — ThemeProvider/useTheme)
    - app/src/components/ui/StatCard.tsx and EmptyState.tsx (07-01 primitives)
    - .planning/phases/07-dual-identity-ui-refresh/07-01-SUMMARY.md
  </read_first>
  <action>
    SURGICAL EDITS ONLY — do not rewrite the file or change unrelated tab components.

    1) Shell: Wrap the dashboard's returned JSX in `<ThemeProvider>`. Replace the inline top header (~52-117) and inline <nav> sidebar (~123-186) with `<AppShell groups={NAV_GROUPS} activeId={activeTab} onSelect={(id) => setActiveTab(id as typeof activeTab)} topBarLeft={<brand+venue switcher>} topBarRight={<view-site links + avatar + logout>}>`. Move the existing nav group/item array into a `NAV_GROUPS` const reused as the AppShell `groups` prop (preserve all current tab ids, labels, and lucide icons). Render the existing `{activeTab === ... && <XTab/>}` content block as AppShell children unchanged.

    2) Dark mode: AppShell already renders the toggle (from 07-01). No extra toggle needed here — just ensure the tree is inside ThemeProvider. Verify the operator background/cards read from --op-* tokens for the shell chrome (tab content panels may keep current styling for this phase).

    3) Activity feed: Add a `trpc.venue.getActivityFeed.useQuery({ token }, { enabled: !!token })`. Render an activity-feed widget (last 5 orders as a compact list with orderNumber + status + amount, plus an "unread reviews" badge showing the count) in the topBarRight area (a notification bell with a numeric badge) OR as a panel on the overview tab — choose the top-bar bell with a dropdown/panel. The bell badge shows unreadReviews count when > 0.

    4) Analytics 7d/30d: In AnalyticsTab, add a `const [rangeDays, setRangeDays] = useState<7 | 30>(30)` and a two-button toggle (7d / 30d). Pass `days: rangeDays` to the existing getDailyRevenue / getTopItems / getOverview queries (replace any hardcoded days value). Ensure a top-5 items table renders from getTopItems (limit 5). Keep the existing recharts revenue chart.

    PRESERVE every existing tRPC call and tab component. The ONLY backend addition consumed here is trpc.venue.getActivityFeed (Task 1).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/src/pages/OwnerDashboard.tsx','utf8'); const f=[['AppShell used', c.includes('AppShell')],['ThemeProvider wraps', c.includes('ThemeProvider')],['activity feed wired', c.includes('getActivityFeed')],['range toggle', c.includes('rangeDays')||/days:\s*(7|30|range)/.test(c)],['top items still queried', c.includes('getTopItems')]]; const bad=f.filter(x=>!x[1]).map(x=>x[0]); if(bad.length){console.error('FAIL',bad);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - OwnerDashboard.tsx imports and renders `AppShell` and wraps its tree in `ThemeProvider`
    - The inline 240px `<nav>` block (old sidebar) and the inline 56px top `<header>` are no longer present as standalone JSX (replaced by AppShell); grep for the old `width: 240` nav inline block returns no match in OwnerDashboard.tsx
    - OwnerDashboard.tsx contains `getActivityFeed` and renders an unread-reviews badge count
    - AnalyticsTab contains a 7d/30d range control (`rangeDays` state) and passes it as `days` to getDailyRevenue/getTopItems
    - A top-5 items table is rendered from getTopItems (limit 5)
    - `npm run build` (vite build) completes without errors
  </acceptance_criteria>
  <done>OwnerDashboard runs inside AppShell with collapsible sidebar + dark mode, shows an activity feed with unread-review badge, and Analytics supports 7d/30d with a top-5 items table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>OwnerDashboard re-shelled with the operator Dual Identity look: collapsible sidebar (240px ↔ 56px icon-only), dark mode toggle persisted to localStorage, top-bar activity feed with unread-review badge, and 7d/30d analytics with a top-5 items table.</what-built>
  <how-to-verify>
    1. Run `npm run dev:all` and log in as an owner, navigate to /dashboard.
    2. Confirm the sidebar collapse toggle shrinks the sidebar to icon-only (~56px) and the choice survives a page refresh.
    3. Toggle dark mode in the top bar; confirm operator chrome recolors and the setting survives a refresh.
    4. Open the Analytics tab; switch between 7d and 30d and confirm the revenue chart and the top-5 items table update.
    5. Confirm the activity feed shows recent orders and the unread-review badge shows a count.
    6. Spot-check a few other tabs (Menu, Orders, Reviews) still load and function (no regressions).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- getActivityFeed added without altering existing procedures
- AppShell + ThemeProvider integrated; old inline sidebar/header removed
- Dark mode + collapse persist across refresh (localStorage)
- Analytics 7d/30d toggle drives chart + top-5 table
- `npm run build` succeeds
</verification>

<success_criteria>
- Success Criterion 1: persistent left sidebar with icon+label nav collapsing to icon-only — MET via AppShell/SidebarNav
- Success Criterion 2: Analytics revenue line chart 7d/30d + top-5 items table — MET
- Success Criterion 7: dark mode toggle persists to localStorage across operator pages — MET (shell-level)
- Activity feed feature delivered (last 5 orders + unread review badge)
- No existing tRPC call or tab regressed
</success_criteria>

<output>
Create `.planning/phases/07-dual-identity-ui-refresh/07-02-SUMMARY.md` when done.
</output>
